# 🔐 Режим разработчика - Руководство пользователя

## Что это?

**Режим разработчика** - это встроенная AI-консоль для администраторов TrustCheck, доступная прямо на главной странице сайта. Никаких отдельных URL - всё в одном месте!

## 🎯 Ключевые возможности

✅ **Вход:** `admin / admin` (по умолчанию)  
✅ **Persistent Session:** Сессия сохраняется 30 дней - не вылетает при перезагрузке!  
✅ **Floating Button:** Кнопка "Developer" в правом нижнем углу на главной странице  
✅ **Full-Screen Console:** Развернутый режим разработчика с AI-чатом  
✅ **Документы → Обучение:** Загрузка файлов автоматически запускает обучение ИИ  
✅ **URL → Web Scraping:** Ссылки в документах триггерят парсинг интернета

---

## 🚀 Как использовать

### Вариант 1: Кнопка на главной странице (Рекомендуется)

1. **Откройте главную страницу:** https://trustcheck.co.il
2. **Найдите кнопку** в правом нижнем углу:
   ```
   [💻 Developer]  ← Фиолетовая круглая кнопка
   ```
3. **Нажмите кнопку** → откроется форма входа
4. **Введите пароль:**
   - Имя: `admin` (фиксированное)
   - Пароль: `admin` (по умолчанию, меняется в `.env`)
5. **Готово!** Режим разработчика активирован

### Вариант 2: Прямая ссылка (Старый способ)

Просто откройте: https://trustcheck.co.il/admin

---

## 🎨 Интерфейс режима разработчика

После входа откроется **полноэкранная консоль разработчика** с тремя вкладками:

### 1️⃣ Вкладка "💬 AI Chat"

**Что делает:**
- Чат с AI ассистентом TrustCheck
- Использует RAG (Retrieval-Augmented Generation)
- Ответы основаны на загруженных документах
- Показывает источники информации

**Как использовать:**
```
Пример вопроса: "Какие документы я загрузил про налоги в Израиле?"
→ AI ответит, указав источники из ваших документов
```

**Загрузка файлов:**
1. Нажмите кнопку "📎 Добавить файлы"
2. Выберите документы (PDF, TXT, DOC, DOCX, MD)
3. Файлы автоматически:
   - Загружаются
   - Обрабатываются (извлечение текста)
   - Векторизуются (добавление в базу знаний)
   - URL из файлов извлекаются и скрапятся

### 2️⃣ Вкладка "📚 Документы"

**Что показывает:**
- Список всех загруженных документов
- Статус обработки (готово/обработка/ошибка)
- Извлечённые URL из каждого документа
- Кнопки управления

**Действия:**
- **🎓 Создать датасет** - генерирует training dataset (JSONL) для файнтюнинга
- **🗑️ Удалить** - удаляет документ и его данные из базы

**Пример:**
```
Документ: "israeli_tax_2025.pdf"
Статус: ✅ Готово
URLs извлечено: 3
- https://www.gov.il/he/departments/guides/taxes-guide
- https://www.misim.gov.il/
- https://www.btl.gov.il/

[🎓 Создать датасет]  [🗑️ Удалить]
```

### 3️⃣ Вкладка "🎓 Training Status"

**Статистика:**
- **Документов загружено:** 15
- **Векторов в базе:** 327
- **Сообщений в истории:** 42
- **Training операций:** 8
- **URLs обработано:** 23

---

## 🤖 Автоматическое обучение AI

### Триггер #1: Загрузка документа

**Что происходит при загрузке:**

1. **Извлечение текста** (PDF → текст, DOC → текст, etc.)
2. **Извлечение URL** (регулярное выражение ищет все ссылки)
3. **Векторизация** (текст разбивается на чанки по 500 символов)
4. **Добавление в базу** (чанки сохраняются в `vector_db.json`)
5. **Web Scraping** (если найдены URL - они автоматически парсятся)

**Пример:**
```
Файл: "training_materials.txt"
Содержимое:
  "Новые возможности GPT-4
   Подробнее: https://openai.com/gpt-4"

→ AI извлечёт текст
→ Найдёт URL: https://openai.com/gpt-4
→ Скрапит контент с сайта OpenAI
→ Добавит всё в базу знаний
→ Теперь можешь спросить: "Что нового в GPT-4?"
```

### Триггер #2: URL в документе

**Автоматический веб-скрапинг:**
- Находит все `https://...` ссылки
- Скрапит до 10 URL за раз
- Timeout: 10 секунд на URL
- Извлекает title + контент
- Очищает от HTML тегов
- Добавляет в базу с меткой "web_scraping"

**Ограничения:**
- Максимум 10 URL за один запрос
- Контент обрезается до 5000 символов
- Пропускает неудачные запросы (не ломает весь процесс)

### Триггер #3: Создание Training Dataset

**Кнопка "🎓 Создать датасет":**
1. Берёт все чанки документа
2. Генерирует Q&A пары:
   ```json
   {
     "instruction": "Расскажи о содержимом документа",
     "input": "Что написано в этом файле?",
     "output": "Текст из чанка..."
   }
   ```
3. Сохраняет в JSONL формат
4. Файл: `data/training/{documentId}_training.jsonl`

**Использование:**
```bash
# На сервере (после создания датасета)
cd /root/trustcheck/data/training
ollama create trustcheck:custom -f Modelfile
# (Modelfile содержит путь к JSONL)
```

---

## 🔒 Безопасность

### Persistent Session

**Как работает:**
- После входа создаётся cookie `admin_session`
- Срок действия: **30 дней** (не 24 часа!)
- HttpOnly: true (защита от XSS)
- SameSite: lax (работает при перезагрузке)
- Path: / (доступно на всех страницах)

**Что это значит:**
✅ Вошёл один раз → работаешь 30 дней  
✅ Перезагрузил страницу → остаёшься в системе  
✅ Закрыл браузер → сессия сохранилась  
✅ Только кнопка "Выйти" удаляет сессию

### Проверка безопасности

**Все API защищены:**
```typescript
// Пример из /api/admin/chat/route.ts
const cookieStore = await cookies();
const sessionCookie = cookieStore.get('admin_session');
if (!sessionCookie) {
  return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
}
```

**Файлы в .gitignore:**
```
data/uploads/      # Загруженные документы
data/training/     # Training datasets
data/*.json        # Векторная база, история чатов
```

---

## 📂 Структура данных

### Хранение на сервере

```
/root/trustcheck/data/
├── uploads/                          # Физические файлы
│   ├── uuid1_document.pdf
│   └── uuid2_report.txt
├── training/                         # Training datasets
│   ├── uuid1_training.jsonl
│   └── uuid2_training.jsonl
├── documents.json                    # Метаданные файлов
├── vector_db.json                    # Векторная база (RAG)
├── admin_chat_history.json          # История чатов (100 сообщений)
└── training_logs.json               # Логи операций обучения
```

### Формат данных

**documents.json:**
```json
{
  "documents": [
    {
      "id": "uuid-here",
      "name": "tax_guide.pdf",
      "size": 524288,
      "uploadedAt": "2026-01-05T10:00:00Z",
      "status": "ready",
      "vectorized": true,
      "urlsExtracted": ["https://gov.il/taxes"]
    }
  ]
}
```

**vector_db.json:**
```json
{
  "documents": [
    {
      "id": "chunk-id",
      "content": "Текст чанка 500 символов...",
      "metadata": {
        "documentId": "uuid-here",
        "source": "tax_guide.pdf",
        "chunkIndex": 0,
        "timestamp": "2026-01-05T10:00:00Z"
      }
    }
  ]
}
```

---

## 🛠️ Настройка на сервере

### Переменные окружения

**Обязательные:**
```bash
# /root/trustcheck/.env
ADMIN_PASSWORD=admin              # Пароль для входа (меняй в продакшене!)
OLLAMA_API_URL=https://your-tunnel-url.trycloudflare.com
OLLAMA_MODEL=trustcheck:15b
```

**Проверка:**
```bash
# SSH на сервер
ssh root@46.224.147.252

# Проверить переменные
docker exec trustcheck-app env | grep -E 'ADMIN|OLLAMA'

# Должно показать:
# ADMIN_PASSWORD=admin
# OLLAMA_API_URL=https://...
# OLLAMA_MODEL=trustcheck:15b
```

### Первый запуск

**На локальной машине (Cloudflare Tunnel):**
```powershell
# E:\SBF\
pwsh scripts/START_TUNNEL.ps1
# Копируй URL из вывода (https://xxx.trycloudflare.com)
```

**На сервере:**
```bash
cd /root/trustcheck
nano .env  # Добавь OLLAMA_API_URL=https://xxx.trycloudflare.com

docker compose down
docker compose build --no-cache
docker compose up -d

# Проверка
docker logs trustcheck-app --tail 50
```

---

## 🧪 Тестирование

### Тест 1: Вход в систему

1. Открой: https://trustcheck.co.il
2. Нажми кнопку "Developer" (правый нижний угол)
3. Введи: `admin` / `admin`
4. Должна открыться консоль разработчика

### Тест 2: Загрузка файла

1. Создай файл `test.txt`:
   ```
   TrustCheck Israel - платформа проверки бизнесов.
   Документация: https://github.com/Zasada1980/trustcheck-israel
   Официальный сайт: https://trustcheck.co.il
   ```
2. Вкладка "💬 AI Chat" → кнопка "📎 Добавить файлы"
3. Загрузи `test.txt`
4. Подожди 5-10 секунд (обработка + скрапинг 2 URL)
5. Перейди во вкладку "📚 Документы"
6. Должно показать: "URLs извлечено: 2"

### Тест 3: RAG поиск

1. Вкладка "💬 AI Chat"
2. Спроси: "Где найти документацию TrustCheck?"
3. AI должен ответить с указанием источника (test.txt) и ссылки на GitHub

### Тест 4: Persistent Session

1. Войди в систему
2. Закрой браузер полностью
3. Открой снова: https://trustcheck.co.il
4. Нажми кнопку "Developer"
5. Должен **сразу открыться режим разработчика** (без повторного входа!)

---

## 🐛 Устранение проблем

### Проблема: "Unauthorized" при входе

**Проверка:**
```bash
# SSH на сервер
docker exec trustcheck-app env | grep ADMIN_PASSWORD

# Если пусто:
nano /root/trustcheck/.env
# Добавь: ADMIN_PASSWORD=admin
docker compose restart app
```

### Проблема: "AI service unavailable"

**Причина:** Ollama tunnel не работает

**Решение:**
```powershell
# На локальной машине
pwsh scripts/START_TUNNEL.ps1

# Копируй новый URL
# SSH на сервер, обнови .env:
OLLAMA_API_URL=https://new-url.trycloudflare.com

docker compose restart app
```

### Проблема: Кнопка "Developer" не отображается

**Проверка:**
```bash
# На сервере
docker exec trustcheck-app cat /app/app/page.tsx | grep DeveloperMenu
# Должно показать: import DeveloperMenu from '@/components/DeveloperMenu';
```

**Если нет:**
```bash
cd /root/trustcheck
git pull origin main
docker compose build --no-cache
docker compose up -d
```

### Проблема: Сессия сбрасывается при перезагрузке

**Проверка cookie:**
1. Открой DevTools (F12) → Application → Cookies
2. Найди `admin_session`
3. Проверь параметры:
   - `SameSite: Lax` (не Strict!)
   - `Path: /`
   - `Max-Age: 2592000` (30 дней)

**Если параметры неверные:**
```bash
cd /root/trustcheck
git pull origin main  # Последняя версия с исправлениями
docker compose restart app
```

### Проблема: Файлы не загружаются

**Проверка директорий:**
```bash
# На сервере
ls -la /root/trustcheck/data/uploads/
ls -la /root/trustcheck/data/training/

# Если не существуют:
mkdir -p /root/trustcheck/data/{uploads,training}
chmod 755 /root/trustcheck/data/{uploads,training}
docker compose restart app
```

---

## 📊 Мониторинг

### Логи системы

```bash
# Логи контейнера (последние 50 строк)
docker logs trustcheck-app --tail 50

# Логи в реальном времени
docker logs trustcheck-app -f

# Ошибки AI чата
docker exec trustcheck-app cat /app/.next/server/app/api/admin/chat/route.js
```

### Статистика использования

```bash
# Количество документов
jq '.documents | length' /root/trustcheck/data/documents.json

# Количество векторов (чанков)
jq '.documents | length' /root/trustcheck/data/vector_db.json

# Размер базы данных
du -sh /root/trustcheck/data/

# Последние training операции
jq '.logs[-5:]' /root/trustcheck/data/training_logs.json
```

---

## 🎓 Расширенное использование

### Кастомный Modelfile для Ollama

**Создать на основе training datasets:**
```bash
# На сервере
cd /root/trustcheck/data/training

# Создать Modelfile
cat > Modelfile <<EOF
FROM trustcheck:15b
ADAPTER ./uuid_training.jsonl
PARAMETER temperature 0.7
PARAMETER top_p 0.9
SYSTEM "Ты AI ассистент TrustCheck Israel, специализирующийся на проверке бизнесов в Израиле."
EOF

# Создать кастомную модель
ollama create trustcheck:custom -f Modelfile

# Использовать в .env:
OLLAMA_MODEL=trustcheck:custom
```

### Экспорт всех данных

```bash
# Создать backup всех данных
cd /root/trustcheck
tar -czf backup_$(date +%Y%m%d).tar.gz data/

# Скачать на локальную машину
scp root@46.224.147.252:/root/trustcheck/backup_*.tar.gz ./
```

---

## 📞 Поддержка

**Документация:**
- **Этот файл:** `DEVELOPER_MODE_GUIDE.md`
- **API Reference:** `ADMIN_CHAT_GUIDE.md`
- **Развертывание:** `ADMIN_CHAT_DEPLOY.md`

**Быстрые ссылки:**
- Режим разработчика: https://trustcheck.co.il (кнопка "Developer")
- Старый вход: https://trustcheck.co.il/admin
- GitHub: https://github.com/Zasada1980/trustcheck-israel

---

**Версия документа:** 1.0.0  
**Дата создания:** 05.01.2026  
**Статус:** ✅ Готово к использованию
