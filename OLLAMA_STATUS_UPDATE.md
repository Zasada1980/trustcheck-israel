# Stateless Ollama — минимальная версия для тестирования

Ollama не установлена на диске. **Два варианта:**

## ✅ Вариант 1: Продолжить с Google Gemini (сейчас)

TrustCheck уже использует Google Gemini API:
- ✅ Работает без установки
- ✅ Генерирует отчёты на иврите
- ✅ Интегрирован в веб-приложение

**Просто используйте что есть сейчас.**

## 🚀 Вариант 2: Установить Ollama после обучения

**План:**

1. **Дождаться завершения обучения** (LLaMA Factory)
   - Статус: Qwen2.5-1.5B обучается на TrustCheck Hebrew
   - Время: ~30-40 минут от начала

2. **Экспортировать модель в GGUF**
   ```powershell
   cd E:\LLaMA-Factory
   python -m llamafactory.cli export \
     --model_name_or_path saves/qwen-trustcheck-hebrew/lora/sft \
     --export_dir exports/trustcheck-hebrew \
     --export_size 4 \
     --export_device cpu \
     --export_legacy_format False
   ```

3. **Скачать Ollama** (если интернет позволит)
   - Или установить вручную с флешки
   - Или использовать portable версию

4. **Загрузить модель в Ollama**
   ```powershell
   cp exports/trustcheck-hebrew/model.gguf E:\SBF\ollama\models\
   ollama create trustcheck-hebrew -f Modelfile
   ```

5. **Использовать локально**
   - Без интернета
   - Без облака
   - Экономия денег

## 📊 Сейчас (Google Gemini)

```
TrustCheck → PostgreSQL → Gemini API → JSON отчёт иврит
```

## 📊 После обучения (Ollama Local)

```
TrustCheck → PostgreSQL → Ollama (местный) → JSON отчёт иврит
```

**Выбор:** Продолжить сейчас с Gemini или дождаться обучения для Ollama?
