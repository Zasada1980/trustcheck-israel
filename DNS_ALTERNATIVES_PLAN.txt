═══════════════════════════════════════════════════════════════════
ПЛАН B: Быстрая настройка без Cloudflare Tunnel
═══════════════════════════════════════════════════════════════════

Проблема: Cloudflare Tunnel требует способ оплаты
Решение: Используем обычный DNS + автоматический SSL

═══════════════════════════════════════════════════════════════════
ВАРИАНТ 1: Дождаться активации Cloudflare (РЕКОМЕНДУЕТСЯ)
═══════════════════════════════════════════════════════════════════

MyNames уже настроил nameservers (elsa/todd).
Просто подожди 30-60 минут:

1. MyNames propagation: 15-30 минут
2. Cloudflare detection: 5-15 минут
3. Получишь email: "trustcheck.co.il is now active"

Проверка через 30 минут:
nslookup -type=NS trustcheck.co.il

Ожидаемый результат:
elsa.ns.cloudflare.com
todd.ns.cloudflare.com

Когда увидишь эти nameservers → Cloudflare автоматически активируется!

SSL будет работать автоматически через Cloudflare (Flexible mode).

═══════════════════════════════════════════════════════════════════
ВАРИАНТ 2: Прямой DNS на сервер + Let's Encrypt (БЫСТРО)
═══════════════════════════════════════════════════════════════════

Если не хочешь ждать Cloudflare:

ШАГ 1: Измени DNS в MyNames на прямой IP
───────────────────────────────────────────────────────────────────

В MyNames → ניהול DNS:

Удали старые записи (185.201.150.252 - парковка MyNames)

Добавь:
A       @           46.224.147.252      3600
A       www         46.224.147.252      3600

ШАГ 2: Установи Let's Encrypt SSL на сервере
───────────────────────────────────────────────────────────────────

# Подключись к серверу
ssh -i C:\Users\zakon\.ssh\trustcheck_hetzner root@46.224.147.252

# Установи Certbot
apt update
apt install -y certbot python3-certbot-nginx

# Получи SSL сертификат
certbot --nginx -d trustcheck.co.il -d www.trustcheck.co.il

# Certbot автоматически:
# 1. Проверит, что DNS указывает на сервер
# 2. Создаст SSL сертификат
# 3. Обновит Nginx конфиг
# 4. Настроит автопродление

# Проверь
systemctl reload nginx

ШАГ 3: Проверь работу
───────────────────────────────────────────────────────────────────

curl https://trustcheck.co.il

Должно работать с 🔒 SSL!

═══════════════════════════════════════════════════════════════════
ВАРИАНТ 3: Комбинированный (Cloudflare + Origin Certificate)
═══════════════════════════════════════════════════════════════════

Когда Cloudflare активируется (elsa/todd nameservers работают):

1. Cloudflare → SSL/TLS → Origin Server
2. Create Certificate (15 years validity)
3. Скопируй Certificate + Private Key
4. Установи на сервер:

mkdir -p /etc/ssl/cloudflare
nano /etc/ssl/cloudflare/trustcheck.pem  # Вставь Certificate
nano /etc/ssl/cloudflare/trustcheck.key  # Вставь Private Key
chmod 600 /etc/ssl/cloudflare/trustcheck.key

5. Обнови nginx.conf:

ssl_certificate /etc/ssl/cloudflare/trustcheck.pem;
ssl_certificate_key /etc/ssl/cloudflare/trustcheck.key;

6. Перезагрузи Nginx:

nginx -t
systemctl reload nginx

7. В Cloudflare: SSL/TLS → Full (strict)

═══════════════════════════════════════════════════════════════════
РЕКОМЕНДАЦИЯ
═══════════════════════════════════════════════════════════════════

🏆 ЛУЧШИЙ ВЫБОР: Вариант 1 (дождаться Cloudflare)

Почему:
✅ Бесплатный SSL
✅ DDoS защита
✅ CDN (быстрая загрузка)
✅ Автоматическое обновление сертификатов
✅ WAF (Web Application Firewall)
✅ Analytics

Время ожидания: 30-60 минут от текущего момента

MyNames УЖЕ настроил nameservers (elsa/todd).
Просто жди email от Cloudflare: "trustcheck.co.il is now active"

═══════════════════════════════════════════════════════════════════

Выбери вариант и я помогу настроить!
