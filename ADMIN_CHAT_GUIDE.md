# 🤖 AI Chat Admin Panel - TrustCheck Israel

Полнофункциональная административная панель с AI-чатом, обучением на документах и веб-скрапингом.

## 📋 Возможности

### ✅ Реализованные функции

1. **🔐 Защищенный доступ**
   - Аутентификация по паролю
   - Session-based авторизация (24 часа)
   - Автоматический logout при истечении сессии

2. **💬 AI Chat с RAG**
   - Интеграция с Ollama (trustcheck:15b модель)
   - RAG (Retrieval-Augmented Generation) - поиск в загруженных документах
   - Контекстные ответы на основе документов
   - История чата (последние 100 сообщений)
   - Мультиязычная поддержка (Hebrew, Russian, English)

3. **📚 Управление документами**
   - Загрузка файлов: PDF, TXT, DOC, DOCX, MD
   - Автоматическое извлечение текста
   - Векторизация для быстрого поиска (RAG)
   - Отслеживание статуса обработки
   - Удаление документов с очисткой векторной базы

4. **🔗 Извлечение и обучение на ссылках**
   - Автоматическое извлечение всех URL из документов
   - Веб-скрапинг контента по ссылкам
   - Добавление веб-контента в базу знаний
   - Batch processing (до 10 URL за раз)
   - Timeout protection (10 секунд на URL)

5. **🎓 Система обучения**
   - Создание training датасетов из документов
   - Генерация Q&A пар для fine-tuning
   - Логирование всех операций обучения
   - Экспорт в JSONL формат для Ollama

6. **📊 Мониторинг и статистика**
   - Количество обработанных документов
   - Количество извлеченных ссылок
   - История операций обучения
   - Real-time статус загрузки

## 🚀 Быстрый старт

### 1. Настройка переменных окружения

Добавьте в `.env`:

```env
# Admin Panel
ADMIN_PASSWORD=your_secure_password_here

# Ollama AI (Local or Tunnel)
OLLAMA_API_URL=http://localhost:11434
OLLAMA_MODEL=trustcheck:15b
```

### 2. Запуск Ollama (если локально)

```powershell
# Запустить Ollama сервер
ollama serve

# Проверить доступные модели
ollama list

# Если нужно, загрузить trustcheck:15b
ollama pull trustcheck:15b
```

### 3. Доступ к админ-панели

1. Откройте браузер: `https://trustcheck.co.il/admin`
2. Введите пароль администратора
3. Готово! 🎉

## 📖 Инструкция по использованию

### Вкладка "💬 AI Chat"

**Основные функции:**

1. **Диалог с AI**
   - Введите вопрос в поле ввода
   - AI использует загруженные документы для контекстных ответов
   - Источники информации указываются в ответе

2. **Загрузка документов**
   - Нажмите "Загрузить документ"
   - Выберите файлы (PDF, TXT, DOC, DOCX, MD)
   - Документ автоматически:
     * Обрабатывается
     * Векторизуется для RAG
     * Извлекаются все ссылки
     * Запускается обучение на веб-контенте (если есть ссылки)

3. **Системные сообщения**
   - Желтые блоки - уведомления системы
   - Показывают статус обработки
   - Информируют о найденных ссылках

### Вкладка "📚 Documents"

**Управление документами:**

1. **Просмотр документов**
   - ✅ Зеленая галочка - документ готов
   - ⏳ Часы - обработка в процессе
   - ❌ Красный крест - ошибка

2. **Информация о документе**
   - Название и размер файла
   - Дата загрузки
   - Количество извлеченных ссылок
   - Статус векторизации

3. **Действия с документами**
   - 🎓 **Обучить** - создать training датасет из документа
   - 🗑️ **Удалить** - удалить документ и его данные

### Вкладка "🎓 Training Status"

**Мониторинг обучения:**

1. **Статистика**
   - Документов обработано
   - Ссылок извлечено

2. **Информация о системе**
   - Как работает загрузка документов
   - Процесс извлечения текста
   - Анализ и обработка ссылок
   - Веб-скрапинг
   - Векторизация для RAG
   - Дообучение модели

## 🛠️ API Endpoints

### Аутентификация

```typescript
// Вход в систему
POST /api/admin/auth/login
Body: { password: string }

// Проверка сессии
GET /api/admin/auth/check

// Выход
POST /api/admin/auth/logout
```

### Чат

```typescript
// Отправить сообщение
POST /api/admin/chat
Body: { message: string, history: Message[] }

// Получить историю
GET /api/admin/chat/history
```

### Документы

```typescript
// Загрузить документ
POST /api/admin/documents/upload
Body: FormData { file: File }

// Получить список документов
GET /api/admin/documents

// Удалить документ
DELETE /api/admin/documents/{id}
```

### Обучение

```typescript
// Обучение на веб-ссылках
POST /api/admin/training/urls
Body: { documentId: string, urls: string[] }

// Создать training датасет из документа
POST /api/admin/training/document
Body: { documentId: string }
```

## 📁 Структура данных

```
data/
├── uploads/                    # Загруженные файлы
│   └── {uuid}_{filename}
├── vector_db.json             # Векторная база для RAG
├── documents.json             # Метаданные документов
├── admin_chat_history.json    # История чата
├── training_logs.json         # Логи обучения
└── training/                  # Training датасеты
    └── {documentId}_training.jsonl
```

### Формат vector_db.json

```json
{
  "documents": [
    {
      "content": "текст чанка (500 символов)",
      "metadata": {
        "documentId": "uuid",
        "title": "название документа",
        "source": "document | web_scraping",
        "chunkIndex": 0,
        "uploadedAt": "ISO date"
      }
    }
  ]
}
```

### Формат documents.json

```json
{
  "documents": [
    {
      "id": "uuid",
      "name": "filename.pdf",
      "size": 123456,
      "uploadedAt": "ISO date",
      "filePath": "/path/to/file",
      "status": "ready | processing | error",
      "vectorized": true,
      "urlsExtracted": ["url1", "url2"],
      "textLength": 5000
    }
  ]
}
```

## 🔧 Технические детали

### RAG (Retrieval-Augmented Generation)

1. **Индексация документов**
   - Текст разбивается на чанки по 500 символов
   - Каждый чанк сохраняется с метаданными
   - Создается индекс для быстрого поиска

2. **Поиск контекста**
   - При запросе выполняется keyword search
   - Находятся топ-3 релевантных чанка
   - Контекст добавляется в промпт для AI

3. **Генерация ответа**
   - Ollama использует контекст из документов
   - Модель генерирует точный ответ
   - Указываются источники информации

### Веб-скрапинг

1. **Извлечение ссылок**
   - Regex поиск всех URL в тексте
   - Дедупликация (удаление дубликатов)
   - Фильтрация валидных HTTP/HTTPS ссылок

2. **Скрапинг контента**
   - User-Agent header для обхода блокировок
   - Timeout 10 секунд на URL
   - Удаление HTML тегов, скриптов, стилей
   - Извлечение только текстового контента

3. **Добавление в базу знаний**
   - Скрапленный текст векторизуется
   - Добавляется в vector_db.json
   - Помечается как source: "web_scraping"

### Обучение модели

1. **Создание датасета**
   - Генерация Q&A пар из документа
   - Формат: instruction, input, output
   - Экспорт в JSONL для Ollama

2. **Fine-tuning (вручную)**
   ```bash
   # Создать Modelfile
   FROM trustcheck:15b
   ADAPTER /path/to/training_data.jsonl
   
   # Создать новую модель
   ollama create trustcheck:custom -f Modelfile
   ```

## ⚠️ Важные замечания

### Безопасность

1. **ОБЯЗАТЕЛЬНО смените ADMIN_PASSWORD в production!**
   ```env
   ADMIN_PASSWORD=strong_random_password_here
   ```

2. **Ограничения доступа**
   - Доступ только через `/admin` роут
   - Session cookies с httpOnly
   - 24-часовая сессия
   - Автоматический logout

### Производительность

1. **Лимиты загрузки**
   - Максимум 10 MB на файл
   - Batch processing для веб-ссылок (макс 10)
   - Timeout 10 секунд на URL

2. **Оптимизация RAG**
   - Топ-3 релевантных чанка
   - Чанки по 500 символов
   - Кэширование в памяти (можно добавить Redis)

3. **Ollama настройки**
   - num_predict: 300 токенов
   - temperature: 0.7
   - timeout: 45 секунд

### Расширения (TODO)

- [ ] Semantic search вместо keyword search
- [ ] PostgreSQL вместо JSON файлов
- [ ] Redis кэширование
- [ ] Batch fine-tuning через Ollama API
- [ ] Embedding vectors для точного поиска
- [ ] Multi-user support с ролями
- [ ] Rate limiting для защиты от DDoS

## 🐛 Troubleshooting

### "AI service unavailable"

**Решение:**
1. Проверьте, запущен ли Ollama: `ollama list`
2. Проверьте OLLAMA_API_URL в `.env`
3. Если используете tunnel, убедитесь что он активен

### "Upload failed"

**Решение:**
1. Проверьте права на запись в `data/uploads/`
2. Убедитесь что файл < 10 MB
3. Поддерживаемые форматы: PDF, TXT, DOC, DOCX, MD

### "Failed to scrape URLs"

**Решение:**
1. Проверьте интернет соединение
2. Некоторые сайты блокируют скрапинг
3. Timeout может быть недостаточен (увеличьте до 30 сек)

### "Unauthorized" при доступе к /admin

**Решение:**
1. Очистите cookies браузера
2. Проверьте ADMIN_PASSWORD в `.env`
3. Перезапустите сервер Next.js

## 📚 Дополнительные ресурсы

- **Ollama документация**: https://ollama.ai/docs
- **RAG guide**: https://python.langchain.com/docs/use_cases/question_answering/
- **Next.js API routes**: https://nextjs.org/docs/app/building-your-application/routing/route-handlers

## 🎯 Примеры использования

### Пример 1: Загрузка документа с бизнес-правилами

1. Загрузите PDF с правилами израильского налогообложения
2. Система извлечет текст и все ссылки на gov.il
3. AI сможет отвечать на вопросы типа:
   - "Какие требования к ח.פ. номерам?"
   - "Как проверить статус компании?"
   - "Где найти данные о мע"מ?"

### Пример 2: Обучение на веб-ресурсах

1. Загрузите документ со ссылками на data.gov.il
2. Система автоматически:
   - Извлечет все URL
   - Скачает контент каждой страницы
   - Добавит в базу знаний
3. AI будет знать актуальную информацию с этих сайтов

### Пример 3: Создание кастомной модели

1. Загрузите 10-20 документов по теме
2. Нажмите "🎓 Обучить" на каждом
3. Соберите все training файлы
4. Создайте fine-tuned модель в Ollama
5. Используйте новую модель в `.env`

---

**Версия:** 1.0.0  
**Дата:** 05.01.2026  
**Статус:** ✅ Production Ready
