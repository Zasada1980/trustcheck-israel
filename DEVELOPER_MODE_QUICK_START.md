# ✅ РЕЖИМ РАЗРАБОТЧИКА АКТИВИРОВАН

## 🎉 Что сделано:

### 1. **Floating кнопка "Developer"** на главной странице
   - Расположение: правый нижний угол
   - Всегда видна (не нужно заходить на `/admin`)
   - При наведении разворачивается с текстом "Developer"

### 2. **Persistent Session (30 дней)**
   - Вход: `admin / admin` (по умолчанию)
   - Сессия НЕ вылетает при:
     - Перезагрузке страницы
     - Закрытии браузера
     - Перезагрузке сервера
   - Срок действия: **30 дней** (не 24 часа!)

### 3. **Полноэкранная консоль разработчика**
   - Открывается после входа
   - Все функции AdminChat работают:
     - 💬 AI Chat с RAG
     - 📚 Документы (загрузка, обработка, удаление)
     - 🎓 Training Status (статистика обучения)
   - Кнопка "Закрыть" возвращает на главную страницу (без выхода)
   - Кнопка "Выйти" удаляет сессию

### 4. **Автоматическое обучение AI**
   - **Загрузка файла** → автоматически:
     - Извлекает текст (PDF, TXT, DOC, DOCX, MD)
     - Находит все URL в файле
     - Скрапит контент по ссылкам (до 10 URL)
     - Добавляет всё в базу знаний (векторизация)
   - **Чат с AI** → использует загруженные документы (RAG)
   - **Создание датасета** → кнопка "🎓 Создать датасет" генерирует JSONL для fine-tuning

---

## 🚀 Как использовать:

### Вариант 1: Кнопка на главной странице (РЕКОМЕНДУЕТСЯ)

1. **Откройте:** https://trustcheck.co.il
2. **Найдите кнопку** в правом нижнем углу:
   ```
   [💻 Developer]  ← Фиолетовая круглая кнопка
   ```
3. **Нажмите кнопку** → откроется форма входа
4. **Введите:**
   - Имя: `admin` (автоматически)
   - Пароль: `admin`
5. **Готово!** Открылась консоль разработчика

### Вариант 2: Прямая ссылка (Старый способ)

Просто откройте: https://trustcheck.co.il/admin

---

## 🧪 Тестирование (3 минуты):

### Тест 1: Вход в систему (30 секунд)
```
✅ Открыть https://trustcheck.co.il
✅ Нажать кнопку "Developer" (правый нижний угол)
✅ Ввести: admin / admin
✅ Должна открыться консоль разработчика
```

### Тест 2: Persistent Session (1 минута)
```
✅ Войти в систему (тест 1)
✅ Закрыть браузер полностью
✅ Открыть снова: https://trustcheck.co.il
✅ Нажать кнопку "Developer"
✅ Должна СРАЗУ открыться консоль (без повторного входа!)
```

### Тест 3: Загрузка файла с URL (1 минута)
```
✅ Войти в систему
✅ Вкладка "💬 AI Chat"
✅ Создать файл test.txt:
   TrustCheck Israel - платформа проверки бизнесов.
   Документация: https://github.com/Zasada1980/trustcheck-israel
   Сайт: https://trustcheck.co.il
✅ Нажать "📎 Добавить файлы" → загрузить test.txt
✅ Подождать 10 секунд (обработка + скрапинг 2 URL)
✅ Вкладка "📚 Документы" → должно показать "URLs извлечено: 2"
```

### Тест 4: RAG поиск (30 секунд)
```
✅ После теста 3
✅ Вкладка "💬 AI Chat"
✅ Спросить: "Где найти документацию TrustCheck?"
✅ AI должен ответить с указанием источника (test.txt) и ссылкой на GitHub
```

---

## 📂 Что развернуто на сервере:

### Структура файлов:
```
/root/trustcheck/
├── data/
│   ├── uploads/              # Загруженные файлы (пусто)
│   ├── training/             # Training datasets (пусто)
│   ├── documents.json        # Метаданные файлов
│   ├── vector_db.json        # Векторная база (RAG)
│   ├── admin_chat_history.json  # История чатов
│   └── training_logs.json    # Логи обучения
├── components/DeveloperMenu.tsx  # Floating button + консоль
├── app/page.tsx              # Главная страница (интегрирован DeveloperMenu)
└── .env                      # ADMIN_PASSWORD=admin
```

### Переменные окружения (.env):
```bash
ADMIN_PASSWORD=admin              # Пароль для входа
OLLAMA_API_URL=https://...        # Cloudflare Tunnel (если есть)
OLLAMA_MODEL=trustcheck:15b       # Модель AI
```

---

## 🛠️ Если что-то не работает:

### Проблема: Кнопка "Developer" не видна

**Решение:**
```bash
# Проверить на сервере
ssh root@46.224.147.252
docker logs trustcheck-app --tail 30

# Перезапустить контейнер
docker compose restart app
```

### Проблема: "Unauthorized" при входе

**Решение:**
```bash
# SSH на сервер
ssh root@46.224.147.252

# Проверить переменную
docker exec trustcheck-app env | grep ADMIN_PASSWORD
# Если пусто:
echo "ADMIN_PASSWORD=admin" >> /root/trustcheck/.env
docker compose restart app
```

### Проблема: Сессия сбрасывается при перезагрузке

**Причина:** Старая версия кода (SameSite: strict)

**Решение:** Уже исправлено в коммите ffbb3fc (SameSite: lax)

---

## 📞 Документация:

- **Этот файл:** `DEVELOPER_MODE_QUICK_START.md`
- **Полное руководство:** `DEVELOPER_MODE_GUIDE.md` (500+ строк)
- **API Reference:** `ADMIN_CHAT_GUIDE.md`
- **Развертывание:** `ADMIN_CHAT_DEPLOY.md`

---

## 🎯 Что дальше:

1. **Протестируйте** систему (3 теста выше)
2. **Загрузите документы** для обучения AI
3. **Смените пароль** (в `.env` на сервере)
4. **Опционально:** Подключите Ollama tunnel для AI чата

---

**Статус:** ✅ Развернуто на https://trustcheck.co.il  
**Commit:** ffbb3fc  
**Дата:** 05.01.2026  
**Версия:** 1.0.0
